#include <cstdlib>
#include <iostream>

using namespace std;

int main()
{
    int l,suma=0;

    while (cin>>l) {
          suma+=l;
          cout<<suma<<endl;
          }
    system("pause");
    return 0;
}
