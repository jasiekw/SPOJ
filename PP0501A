#include <iostream>
using namespace std;

int main() {
	
	 int a,b,ile;

    cin>>ile;

    for (int i=0;i<ile;i++)
    {
        cin>>a>>b;

        while (a!=b)
            {
            if (a>b) a=a-b;
            else b=b-a;
            }
        cout<<a<<endl;
    }

	return 0;
}
