#include <iostream>

using namespace std;

int main()
{
    int ile, powt;

    cin>>powt;

    for (int p=0;p<powt;p++)
        {
        cin >>ile;

        int tab[ile];

        for (int i=0;i<ile;i++)cin>>tab[i];

        for (int i=1;i<ile;i++)cout<<tab[i]<<" ";
        cout<<tab[0];
        cout<<endl;
        }

    return 0;
}
